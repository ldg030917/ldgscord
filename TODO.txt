해결했지만 수정 필요: --
급함: #

--서버 만들기 기능 추가
--대화 기능 추가
--프론트 수정
#channels/@me에 친구 기능 추가
서버 삭제 기능
--채널 생성 
--채널 삭제
channels/@me에서 재시작 시 에러 생기는 문제 수정

메시지 전송 시 url을 제멋대로 설정하면 존재하지 않는 메시지 생성 가능
=>서버, 채널 id 확인하고 없으면 에러 전송

다른 사람이 채널 내에 없어도 url만 알면 메시지 전송 가능
=>auth 과정 추가?

#serverinfo 테이블에 참가자를 유저 이름이 아닌 유저 id로 변경 -> 유저 이름은 중복 가능..


cloneNode 함수를 이용해 dom 객체 복사하여 코드 간결, 복잡한 구조 구현?

만약 서버에 채팅 채널이 없을 경우, 없다는 것 표현?

채널 하나도 없을 시 url 변경 안됨, 없는 채널에 채팅을 쳐 db 쿼리 이슈가 생길 수 있음.

채널이 하나도 없는 서버에서 채널 추가 시, 다른 서버에서 채널이 추가되는 이슈 -> 채널 없는 서버는 url 변경이 안되는 문제!!!

Front는 안하고싶다

usertable 고치기(최적화) -> 친구 초대

인당 dm 채널이 추가되는 형식인듯

서버에 따른 유저 권한 테이블 만들기

메시지 불러올 때 페이징 처리(가장 최근 50개의 메시지만 불러오는 방식?)